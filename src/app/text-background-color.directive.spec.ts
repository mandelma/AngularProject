import { TextBackgroundColorDirective } from './text-background-color.directive';

describe('TextBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextBackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
