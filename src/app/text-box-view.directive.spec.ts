import { TextBoxViewDirective } from './text-box-view.directive';

describe('TextBoxViewDirective', () => {
  it('should create an instance', () => {
    const directive = new TextBoxViewDirective();
    expect(directive).toBeTruthy();
  });
});
